import styled from 'styled-components';
import {
  color,
  space,
  layout,
  flexbox,
  background,
  border,
  position,
  typography,
  shadow,
} from 'styled-system';

const Box = styled('div')(
  color,
  space,
  layout,
  flexbox,
  background,
  border,
  position,
  typography,
  shadow
);

export default Box;
